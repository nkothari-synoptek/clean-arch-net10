apiVersion: v1
kind: ConfigMap
metadata:
  name: api-gateway-config
  namespace: digital-inspection
  labels:
    app: api-gateway
    component: gateway
data:
  azure-ad-authority: "https://login.microsoftonline.com/YOUR_TENANT_ID"
  azure-ad-client-id: "YOUR_CLIENT_ID"
  azure-ad-tenant-id: "YOUR_TENANT_ID"
