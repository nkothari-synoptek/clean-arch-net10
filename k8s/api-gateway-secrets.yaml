# This is a template file. DO NOT commit actual secrets to version control.
# Use Azure Key Vault, Kubernetes Secrets, or a secrets management tool.
apiVersion: v1
kind: Secret
metadata:
  name: api-gateway-secrets
  namespace: digital-inspection
  labels:
    app: api-gateway
    component: gateway
type: Opaque
stringData:
  appinsights-connection-string: "REPLACE_WITH_ACTUAL_APPINSIGHTS_CONNECTION_STRING"
